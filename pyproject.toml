[build-system]
requires = ["hatchling>=1.29"]
build-backend = "hatchling.build"

[project]
name = "commit-with-ai"
version = "1.2.0"
description = "AI-powered git commit message generator supporting multiple providers (Gemini, Claude, OpenAI Codex)"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Chen-Wei Sun", email = "chenwei791129@livemail.tw"}
]
keywords = ["git", "commit", "ai", "gemini", "claude", "openai", "codex", "cli", "conventional-commits"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Version Control :: Git",
    "Topic :: Utilities",
]
dependencies = [
    "google-genai>=1.68.0",
    "httpx>=0.28.1",
]

[project.urls]
Homepage = "https://github.com/chenwei791129/commit-with-ai"
Repository = "https://github.com/chenwei791129/commit-with-ai"
"Bug Tracker" = "https://github.com/chenwei791129/commit-with-ai/issues"
Changelog = "https://github.com/chenwei791129/commit-with-ai/releases"

[project.scripts]
commit-with-ai = "commit_with_ai.__main__:main"

[project.optional-dependencies]
dev = [
    "ruff>=0.15.7",
    "pytest>=9.0.2",
]

[tool.hatchling.build.targets.wheel]
packages = ["commit_with_ai"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "C4", "SIM"]
ignore = ["E501"]
